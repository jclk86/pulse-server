CREATE TABLE travelist_comments (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  user_id INTEGER REFERENCES travelist_users(id) ON DELETE CASCADE NOT NULL,
  article_id INTEGER REFERENCES travelist_articles(id) ON DELETE CASCADE NOT NULL,
  content TEXT NOT NULL,
  date_created TIMESTAMP DEFAULT now() NOT NULL
);

